{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/TimerSubtract",
  "definitions": {
    "TimerSubtract": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "subtract"
          ],
          "description": "The message name — <name> in the routing key."
        },
        "domain": {
          "type": "string",
          "enum": [
            "timer"
          ],
          "description": "The owning domain — <domain> in the routing key."
        },
        "kind": {
          "type": "string",
          "enum": [
            "command"
          ],
          "description": "Which exchange carries this message. Never reaches the routing key."
        },
        "ts": {
          "type": "string",
          "format": "date-time",
          "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
          "description": "The Timer manager's authoritative server timestamp. Every broadcast is an anchor rather than a tick, so a client reconciles its own clock against this before interpolating."
        },
        "target": {
          "type": "string",
          "minLength": 1,
          "description": "The timer instance this message addresses — the <target> routing segment. Required on every timer message: a timer is a keyed instance, and 'presentation-phase' is the well-known key of the standing one."
        },
        "delta": {
          "type": "integer",
          "minimum": -9007199254740991,
          "maximum": 9007199254740991,
          "description": "Seconds to subtract."
        }
      },
      "required": [
        "type",
        "domain",
        "kind",
        "ts",
        "target",
        "delta"
      ],
      "additionalProperties": false,
      "description": "Take time away. The mirror of add, and likewise does not re-anchor startingValue."
    }
  }
}
