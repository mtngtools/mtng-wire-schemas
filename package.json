{
  "name": "@mtngtools/mtng-wire-schemas",
  "version": "0.1.1",
  "description": "Shared wire contract (Zod source of truth) between mtng-mono (TS) and mtng-dotnet-mono (.NET).",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "node -e \"console.error('generate: the Zod -> JSON Schema command chain is finalized by the acceptance spike in mtng-dotnet-mono (foundation map). schemas/ is stubbed until then.'); process.exit(1)\""
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "ts-json-schema-generator": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
